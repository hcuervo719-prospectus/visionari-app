// ============================================================
// INSTRUCCIONES DE DESPLIEGUE - PÁGINAS SERVER COMPONENTS 2-8
// Crea cada archivo en la ruta indicada y pega el contenido
// ============================================================

// ─── app/[locale]/dashboard/component-2/page.tsx ───────────
/*
import { getLocale } from 'next-intl/server';
import Component2Content from '@/components/Component2Content';

export default async function Component2Page() {
  const locale = await getLocale();
  return <Component2Content locale={locale} />;
}
*/

// ─── app/[locale]/dashboard/component-3/page.tsx ───────────
/*
import { getLocale } from 'next-intl/server';
import Component3Content from '@/components/Component3Content';

export default async function Component3Page() {
  const locale = await getLocale();
  return <Component3Content locale={locale} />;
}
*/

// ─── app/[locale]/dashboard/component-4/page.tsx ───────────
/*
import { getLocale } from 'next-intl/server';
import Component4Content from '@/components/Component4Content';

export default async function Component4Page() {
  const locale = await getLocale();
  return <Component4Content locale={locale} />;
}
*/

// ─── app/[locale]/dashboard/component-5/page.tsx ───────────
/*
import { getLocale } from 'next-intl/server';
import Component5Content from '@/components/Component5Content';

export default async function Component5Page() {
  const locale = await getLocale();
  return <Component5Content locale={locale} />;
}
*/

// ─── app/[locale]/dashboard/component-6/page.tsx ───────────
/*
import { getLocale } from 'next-intl/server';
import Component6Content from '@/components/Component6Content';

export default async function Component6Page() {
  const locale = await getLocale();
  return <Component6Content locale={locale} />;
}
*/

// ─── app/[locale]/dashboard/component-7/page.tsx ───────────
/*
import { getLocale } from 'next-intl/server';
import Component7Content from '@/components/Component7Content';

export default async function Component7Page() {
  const locale = await getLocale();
  return <Component7Content locale={locale} />;
}
*/

// ─── app/[locale]/dashboard/component-8/page.tsx ───────────
/*
import { getLocale } from 'next-intl/server';
import Component8Content from '@/components/Component8Content';

export default async function Component8Page() {
  const locale = await getLocale();
  return <Component8Content locale={locale} />;
}
*/
